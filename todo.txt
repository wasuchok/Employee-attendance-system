TODO ที่เหลือ

1. ทดสอบ check-in บนแอปจริง
   - ถ้ายังเจอ 500 ให้ดู field "error" จาก backend response

2. ใช้ location จริงจากมือถือ
   - ตอนนี้ยังใช้พิกัด mock
   - เพิ่ม geolocator, ขอ permission, ส่ง lat/lng จริงตอน check-in

3. เชื่อมหน้า History กับ backend
   - ใช้ GET /api/attendances/history
   - แทน mock data ใน history_page.dart

4. ปรับ Today card ให้ครบ
   - Working time ยังเป็น "-"
   - Office distance ควรใช้ distance_meters จาก backend

5. เก็บ flutter analyze ที่เหลือ
   - ลบหรือ rename home_page copy.dart
   - แก้ withOpacity ใน app_button.dart
   - แก้ widget_test.dart ที่ยังเรียก MyApp

6. ทำ database constraint กัน check-in ซ้ำ
   - เพิ่ม UNIQUE(employee_id, attendance_date)
   - ถ้าชน constraint ให้ backend ตอบ 409

7. ทดสอบ weather icon บนหน้า Home
   - ตอนนี้ดึง weather ระยองจาก Open-Meteo
   - icon เปลี่ยนตามแดด/ฝน/พายุ
